%*********************************************************************
%	CHAPTER 3 - THEORETICAL DEVELOPMENT
%*********************************************************************
\chapter{Theoretical development}
\label{ch:theory}
%---------------------------------------------------------------------
% TODO: ADD CMOS REFERENCE
Modern digital use a sensor matrix to capture incoming light, captured with photosensitive. Modern cameras mostly use complentary metal-oxide semiconductor technology (CMOS) . Each phototransistor makes up a single pixel sensor,
 with sensor matrices being NcolxNrows arrays of phototransistors \cite{klette2014concise}. This section explains the theory behind
 how a camera projects the 3D world scene onto a 2D plane, camera-specific (intrinsic and extrinsic) paramteres affecting this projection, the effects
 of the underwater environment on camera theory and the calibration and compensation used to correct for them.

%=====================================================================
\section{Camera models}
\label{sec:ch3.cameramodels}
% TODO: 23 Sep
%=====================================================================
Camera models do what?
They can be split into central and non-central camera models.

%---------------------------------------------------------------------
\subsection{Central camera models}
%---------------------------------------------------------------------
A central camera has a single optical center that every incoming ray passes through,
Most models are central camera models.

%---------------------------------------------------------------------
\subsection{Non-central camera models}
%---------------------------------------------------------------------
Non-central camera models do not have a single optical center.
(Something about back-propjection, maybe mention later).
\cite{sturmCameraModelsFundamental2011} breaks the non-central cameras into
\begin{itemize}
	\item \textbf{Oblique cameras}: no two rays ever meeet.
	\item \textbf{Axial cameras}: have one line that every ray crosses, even if they dont share a single point.
	\item \textbf{X-slit cameras}: axial cameras with two lines every ray crosses.
\end{itemize}
Important for underwater why...?

%---------------------------------------------------------------------
\subsection{Model parameters: global, local and discrete}
%---------------------------------------------------------------------
\cite{sturmCameraModelsFundamental2011} further seperates models into global, local and discrete
models. This seperation regards how the model's paramters describe the image.
Global models (e.g.\ Pinhole) have parameters for the whole image, local sperates parameters sets
for different section of the image. Discrete models have their projection function sampled at
different points, e.g.\ one per pixel. For discrete models, interpolation neede for back-projecting
to non-sampled image points, essentially making them local models.

%---------------------------------------------------------------------
\subsection{Forward vs back-projection}
%---------------------------------------------------------------------
Forward projection maps a 3D point to a sensor pixel and back-projection does the inverse, mapping
a pixel to a 3D ray. Within the context of this project, forward projection is used for corecting
distorition and backwards projection for estimating a camera pose and reconstruction.

Mathematically, both directions define their invers, but \cite{sturmCameraModelsFundamental2011}
points out that back-projection is generally easier to determine\ldots

The notation used in this report will follow the following notation:
% \rayvec{<index>}: bold B with an extra-raised superscript index
\providecommand{\rayvec}[1]{\bm{B}^{\raisebox{0.45ex}{$\scriptstyle #1$}}}
\begin{description}[leftmargin=4em, labelsep=1em, itemsep=0.3ex]
	\item[$\rayvec{i}$] The ray point at infinity (3-vector in homogenous coordinates).
	\item[$\rayvec{f}$] Finite point on the ray (4-vector in homegnous 3D coordinates).
	\item[$\rayvec{l}$] Ray written as a single $6 \times n$ vector computed from $\rayvec{i}$ and $\rayvec{f}$.
\end{description}

\subsection{Radial distortion}

\subsection{Extrinsic parameters}

\subsection{Intrinsic parameters}

% Reinhard Klette
Intrinsic (camera specific) and extrinsic parameters are required.
Intrinsic: (effective) focal length, dimensions of the sensor matrix, sensor cell size or aspect ratio of sensor height to width, radial distortion parameters, coordinates of the principal point, or the scaling factor.
Extrinsic: applied affine transforms for identifying poses (location and direction) of cameras in a world coordinate system.

Each new placement in the 3D scene defines a different world coordinate system.


%---------------------------------------------------------------------
\subsection{Pinhole model and coordinate projection}
\label{subsec:ch3.pinhole}
% TODO ADD IMAGE
% TODO 23 Sep
%---------------------------------------------------------------------
\
%---------------------------------------------------------------------
\subsection{Intrinsic parameters}
\label{subsec:ch3.intrinsics}
% TODO 23 Sep
%---------------------------------------------------------------------
% Camera matrix K, f_x, f_y, c_x, c_y, skew
% Refine the camera model: different focal length in the x and y direction,
% f_x, f_y. Can also include edge length e_x and e_y of the sensor cells used in
% the sensor matrix in the camera.

%---------------------------------------------------------------------
\subsection{Extrinsic parameters}
\label{subsec:ch3.extrinsics}
% TODO 23 Sep
%---------------------------------------------------------------------
% Rotation and translation of each camera in the world frame

%---------------------------------------------------------------------
\subsection{Lens distortion}
\label{subsec:ch3.distortion}
%---------------------------------------------------------------------
\textbf{Lens Distortion:} the 3D-2D image point mapping combines a perspective projection (deviating from the pinhole camera) caused by radial distortion.
Gives (simplified) two equations to explain.


%=====================================================================
\section{Underwater image formation}
\label{sec:ch3.underwaterimaging}
%=====================================================================

%---------------------------------------------------------------------
\subsection{Snell's law at the water-glass interface}
\label{subsec:ch3.snell}
%---------------------------------------------------------------------

%---------------------------------------------------------------------
\subsection{Refraction versus radial distortion}
\label{subsec:ch3.refraction_vs_distortion}
%---------------------------------------------------------------------
% Why refraction is not truly absorbable into the radial distortion terms

%---------------------------------------------------------------------
\subsection{Compensation approaches}
\label{subsec:ch3.compensation}
%---------------------------------------------------------------------
% - Ignore refraction and absorb into distortion
% - Explicit physical / ray-tracing models
% - The Pinax model
% - Corrective optics

%---------------------------------------------------------------------
\subsection{Wavelength-dependent attenuation and scattering}
\label{subsec:ch3.attenuation}
%---------------------------------------------------------------------
% Why the red channel dies first, backscatter and forward scatter


%=====================================================================
\section{Image conditioning}
\label{sec:ch3.conditioning}
%=====================================================================
The failed detections result from blur due to turbidity, small pixel size at long range, uneven lighting, backscatter and low contrast.

Filtering before detection allows for the recovery of more tags, and more usable calibration frames.

Cannot have any alteration of the geometry of the pixels as this affects the locations of the gradient corners.

%---------------------------------------------------------------------
\subsection{Colour channel selection and filtering}
\label{subsec:ch3.channelselection}
%---------------------------------------------------------------------
% Which makes sense to choose (reference lit review) -> re-explain Y channel
% Compare which channel to extract (Y, G, B, R)
% CLAUDE: it may be important to only calibrate with gray because of some
% conversion that might happen. Must have a chat and try prove this.

All chosen filters take and return the 8-bit grayscale image.

% Unsharp masking (cv2.addWeighted)
% What it does, and basic maths behind how it does it

%---------------------------------------------------------------------
\subsection{Contrast Limited Adaptive Histogram Equalisation (CLAHE)}
\label{subsec:ch3.clahe}
%---------------------------------------------------------------------


%=====================================================================
\section{Camera calibration}
\label{sec:ch3.calibration}
%=====================================================================
\textbf{Designing a Calibration Method:}
% Zhang mentions Three-dimensional reference object-based calibrtaion and Self-calibration seperately
Points $(X_w, Y_w, Z_w)$ on the calibration rig (e.g. corners of the squares) or calibration marks are known by their physically measured coordinates. Determine a corresponding $(x,y)$ if possible with sub-pixel accuracy. That is the projection of $(X_w, Y_w, Z_w)$ in the world plane. E.g. 100 points, 100 equations.

The equations defining our camera model contain $X_w$, $Y_w$, $Z_w$, $x$ and $y$ as known values, and the intrinsic and extrinsic camera parameters as unknowns. The resulting equation system (necessarily nonlinear due to central projection or radial distortion) needs to be solved for the specified unknowns. Over-determined situations provide stability of the numerical solution scheme.

%---------------------------------------------------------------------
\subsection{Calibration targets and AprilTag fiducials}
\label{subsec:ch3.targets}
%---------------------------------------------------------------------
\textbf{Manufacturing a calibration board:}
Rigid B\&W pattern common, at least 7x7 squares large enough such that the minimum size when recorded on the image is at least 10x10 pixels (i.e. having a camera with effective focal length \textit{f}, this allows us to estimate the size \textit{axa} of each square assuming a distance of $b$ m between camera and board).

\textbf{Localising Corners in the Checkerboard:}
 Calibration marks are the corners of squares (approximated by the intersection points of grid lines, thus defining corners potentially with sub-pixel accuracy).

 Mention of the Hough space method for detecting line segments requires that lens distortion has been removed from recorded images prior to applying the Hough-space method. Images with lens distortion will show bended lines rather than straight grid lines.

 \textbf{Localising Calibration marks:}
 Can also be defined by circular or square dots. Popular if same location, can be permanently painted on walls or other static surfaces.

 Sub-pixel accuracy by calculating the centroid of this region (example given).

Common calibration techniques implement checkerboards as explained in SECTION X. Due to the difficulties (leaning off boat) in underwater in-situ calibration, it was chosen to use calibration boards with fiducial markers. Fiducial markers are "landmarks" to aid in computer vision models. Unique IDs are "encoded in their structure following a pre-defined pattern system and provide a means to implement reliable pose estimation of the marker with respect to the camera."

April tag size:
QR tags comprise about 268 pixels (not including required headers or the payload), whereas the visual fiducials described in this paper range from about 49 to 100 pixels, including the payload.
% https://april.eecs.umich.edu/pdfs/olson2011tags.pdf

%---------------------------------------------------------------------
\subsection{AprilTag detection stages}
\label{subsec:ch3.detectionstages}
%---------------------------------------------------------------------
Tags are detected with OpenCV's \texttt{ArucoDetector} using the AprilTag 36h11 dictionary. The detector has four stages:
\begin{enumerate}
	\item \textbf{Thresholding.} The grayscale image is converted to black and white with adaptive thresholding. This compares each pixel to its local neighbourhood rather than to a global level, which is particularly useful for uneven lighting across the board.
	\item \textbf{Candidate search.} Looks for contours indicative of the outer black square of a tag (rough square, 4 sides, reasonable size).
	\item \textbf{Decoding.} Each candidate is morphed into a square shape and the internal white/black grid is read off. Candidates without a valid code are rejected.
	\item \textbf{Output.} For each accepted tag, the detector reports the id and the pixel positions of the corners.
\end{enumerate}

% Comparing detection algorithms
% Compare Tuned OpenCV ArUco, pyapriltag AprilTag3 and ...
% - ArUco3 is fast (downscales) with global thresholds
% - AprilTag2 computes each local threshold, better for uneven lighting and low
%   contrast, but expensive, and doesnt do board calibrations
% Explain maths logic here (flowcharts maybe)
% Maybe even try UWARUco attempt https://www.mdpi.com/2072-4292/11/4/459
% (useful explanation of how it works WITH DIAGRAM)

%---------------------------------------------------------------------
\subsection{Parameter estimation and optimisation}
\label{subsec:ch3.estimation}
%---------------------------------------------------------------------
% Homography initialisation, then non-linear refinement of reprojection error

%---------------------------------------------------------------------
\subsection{Stereo calibration}
\label{subsec:ch3.stereocalibration}
%---------------------------------------------------------------------
% Recovering R, T between the two cameras; fixing or refining the intrinsics


%=====================================================================
\section{Stereo geometry}
\label{sec:ch3.stereogeometry}
%=====================================================================

%---------------------------------------------------------------------
\subsection{Epipolar geometry}
\label{subsec:ch3.epipolar}
%---------------------------------------------------------------------

%---------------------------------------------------------------------
\subsection{Rectification}
\label{subsec:ch3.rectification}
%---------------------------------------------------------------------
 \textbf{Rectification of Stereo Image Pairs:}
 The main complexity is to identify corresponding points in pairs of input images recorded by two cameras. It is convenient to warp the image pairs so that it appears they were recorded in canonical stereo (by a pair of identical cameras). This is called geometric rectification.

%---------------------------------------------------------------------
\subsection{Triangulation and depth from disparity}
\label{subsec:ch3.triangulation}
%---------------------------------------------------------------------


%=====================================================================
\section{Stereo matching}
\label{sec:ch3.stereomatching}
%=====================================================================

%---------------------------------------------------------------------
\subsection{The correspondence problem}
\label{subsec:ch3.correspondence}
%---------------------------------------------------------------------
 Stereo matching is an example of a labelling approach.
 The labelling function \textit{f} gives a label $f_p \in L$ (a disparity) to each pixel location $p \in \Omega$, applying error functions $E_{data}$ and $E_{smooth}$.

  Accumulated costs of a label are the combined data and adjacent smoothness error values.

  Kind of like a neighbourhood of pixels as a box, compared on an epipolar line (same row due to canonical stereo geometry).

  \textbf{Generic Model for Matching:}
  With a left and right image, one is now the base and the other the match image $(B, M)$. For a pixel $(x, y, B(x,y))$ in the base image, search for the corresponding $(x+d, y, M(x+d,y))$ in the match image, on the same epipolar line.

  \textbf{Base and Match Images and Search Intervals:}
  $d$ ends up being the difference along the epipolar line.

  \textbf{Label Set for Stereo Matching:}
  Assumes right-to-left matching to avoid $\pm$ switching. This ends up with the label set $L = \{-1, 0, -1, \dots, -d_{max}\}$. Using the intrinsic and extrinsic camera parameters, $d_{max}$ can be estimated from the closest objects in the scene that are "still of interest".

%---------------------------------------------------------------------
\subsection{Matching cost functions}
\label{subsec:ch3.costfunctions}
%---------------------------------------------------------------------
 \textbf{Neighbourhoods of correspondents:}
 Issues with SSD (sum of squared differences) or SAD (sum of absolute differences) justify the 3D cost matrix.

 \textbf{3D Data Cost Matrix:}
 A comprehensive representation of all the involved data costs, which can be used for further optimisation (using a smoothness-cost function in the control of the \textit{stereo} matching).

 \textbf{Data cost functions:}
 	\begin{itemize}
 		\item Zero-Mean version
 		\item NCC Data Cost
 		\item The Census Data-Cost Function
 	\end{itemize}

%---------------------------------------------------------------------
\subsection{Local to global matching and Semi-Global Matching (SGM)}
\label{subsec:ch3.sgm}
%---------------------------------------------------------------------
 \textbf{From Global to Local Matching:}
 Introduces SGM, which is key for OpenCV.

 Justification for OpenCV over MATLAB with respect to more SGM knobs.

%---------------------------------------------------------------------
\subsection{Disparity refinement and filtering}
\label{subsec:ch3.disparityrefinement}
%---------------------------------------------------------------------
% Left-right consistency check, speckle filtering, WLS


%=====================================================================
\section{Point cloud reconstruction and registration}
\label{sec:ch3.pointcloud}
%=====================================================================

%---------------------------------------------------------------------
\subsection{Disparity to point cloud}
\label{subsec:ch3.disparity2cloud}
%---------------------------------------------------------------------
% Reprojection with Q

%---------------------------------------------------------------------
\subsection{Point cloud filtering}
\label{subsec:ch3.cloudfiltering}
%---------------------------------------------------------------------
% Statistical outlier removal, voxel downsampling

%---------------------------------------------------------------------
\subsection{Feature-based alignment}
\label{subsec:ch3.featurealignment}
%---------------------------------------------------------------------

%---------------------------------------------------------------------
\subsection{Iterative Closest Point (ICP)}
\label{subsec:ch3.icp}
%---------------------------------------------------------------------


%=====================================================================
\section{Error metrics}
\label{sec:ch3.errormetrics}
%=====================================================================

%---------------------------------------------------------------------
\subsection{Reprojection error}
\label{subsec:ch3.reprojectionerror}
%---------------------------------------------------------------------
The metric of corner detections is ...

%---------------------------------------------------------------------
\subsection{Triangulation and depth error}
\label{subsec:ch3.deptherror}
%---------------------------------------------------------------------

%---------------------------------------------------------------------
\subsection{Cloud-to-mesh error}
\label{subsec:ch3.cloud2mesh}
%---------------------------------------------------------------------

%*********************************************************************
% END
%*********************************************************************
